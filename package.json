{
  "name": "moneygousd-monitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "lint": "npx tsc --noEmit",
    "typecheck": "npx tsc --noEmit",
    "tail": "wrangler tail",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241201.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
